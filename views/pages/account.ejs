<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            color: white;
            background-color: #252525;
            text-align: center;
        }

        input[type=submit] {
            background-color: rgb(32, 32, 32);
            color: white;
            border: none;
            border-radius: 4px;
            padding: 0.5rem;
            cursor: pointer;
            transition: background-color 0.2s ease;
            width: 5%;
        }

        input[type=text] {
            background-color: rgb(255, 255, 255);
            color: rgb(0, 0, 0);
            border: none;
            border-radius: 4px;
            padding: 0.5rem;
            cursor: pointer;
            transition: background-color 0.2s ease;
            width: 10%;
        }

        input[type=password] {
            background-color: rgb(255, 255, 255);
            color: rgb(0, 0, 0);
            border: none;
            border-radius: 4px;
            padding: 0.5rem;
            cursor: pointer;
            transition: background-color 0.2s ease;
            width: 10%;
        }

        #AccountSettings {
            display: none;
        }

        #PageSettings {
            display: none;
        }

        #AccountPrivacy {
            display: none;
        }

        #accountSettings {
            width: 100%;
            left: 100%;
        }
    </style>
</head>

<body>
    <h1> Welcome to your account </h1>
    <h2 style="font-style: italic;"> Hello, <%= name %></h2>

    <div class='page'>
        <div class='right-col'>
            <div id="accountSettings">
                <h1> Account Settings </h1>
                <form method="post" action="/link">
                    <h2> Link Mincraft Account (write uuid) </h2>
                    <input type="text" name="uuid" value="uuid" />
                    <input type="hidden" name="username" value="<%= name %>" />
                    <input type="submit" value="link">
                </form>
                <form method="post" action="/changepasswordrequest">
                    <h1> Change Account Details </h1>
                    <h3> Change Password: </h3>
                    <input type="password" name="password_field" />
                    <input type="hidden" name="username" value="<%= name %>" />
                    <h3></h3>
                    <input type="submit" value="save" />
                </form>
            </div>
        </div>
    </div>
</body>

</html>
